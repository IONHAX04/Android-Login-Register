<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.THIRU" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/catamaran_medium</item>
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="CustomSpinnerStyle">
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="TabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:paddingVertical">16dp</item> <!-- Adjust as needed -->
        <item name="android:paddingHorizontal">8dp</item> <!-- Adjust as needed -->
    </style>

    <style name="Theme.THIRU" parent="Base.Theme.THIRU" />


</resources>